import { makeStyles } from "@material-ui/core/styles";
import EndlessConstellations from "../../../img/Endless-Constellation.svg";

export default makeStyles((theme) => ({
  mainSection: {
    backgroundColor: "#330033",
    backgroundImage: `url(${EndlessConstellations})`,
  },
}));
